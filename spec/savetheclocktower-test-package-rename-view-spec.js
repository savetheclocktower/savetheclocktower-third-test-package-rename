'use babel';

import SavetheclocktowerTestPackageRenameView from '../lib/savetheclocktower-test-package-rename-view';

describe('SavetheclocktowerTestPackageRenameView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
